# TODO - Reporting

Goal: Provide clear, comparable benchmark outputs.

Tasks:
- [ ] Implement Option B table output by default.
- [ ] Add tag-based slicing (e.g., metrics for dark_bib only).
- [ ] Include per-photo status (PASS, MISS, PARTIAL).
